<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lost_found_backend.mapper.UserMapper">

    <select id="findByOpenid" resultType="com.example.lost_found_backend.entity.User">
        SELECT id, openid, nickname, avatar_url, phone, role, status, create_time, update_time
        FROM `user`
        WHERE openid = #{openid}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (openid, nickname, avatar_url, phone, role, status, create_time, update_time)
        VALUES (#{openid}, #{nickname}, #{avatarUrl}, #{phone}, 0, 0, NOW(), NOW())
    </insert>

    <update id="updateById">
        UPDATE `user`
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="phone != null">phone = #{phone},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

</mapper>
